spring.application.name=backend
spring.datasource.url=jdbc:mysql://localhost:3306/mesu?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update

# borrar esto para activar el spring security
#spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

# comentar esto para activar el spring security
# Configuracion para testeo con postman
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always

# Cloudinary Configuration
cloudinary.cloud-name=dwyzkdftc
cloudinary.api-key=848553669342279
cloudinary.api-secret=gEzne7qsLTejXnZwhRBXft9TacU